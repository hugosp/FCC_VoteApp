extends layout

block content
    .container
        .row
            for poll in polls
                .column
                    // .question= poll.question
                    canvas(id= poll._id width=300 height=300)
    
    
    
    
    
    
                // ul
                    for ans in poll.answers
                        li= ans.answer
                            span.vote= ans.votes
